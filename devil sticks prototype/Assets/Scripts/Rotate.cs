using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotate : MonoBehaviour
{

    //float rotation;
    float speed = 0.5f;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void RotateObject()
    {
        /*Vector3 currentrRotation = transform.rotation.eulerAngles;
        currentrRotation.z += speed;
        transform.rotation.eulerAngles = currentrRotation;*/
    }
}
